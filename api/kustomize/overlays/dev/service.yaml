apiVersion: v1
kind: Service
metadata:
  name: kotsbots-api
  labels:
    app: kotsbots-api
spec:
  type: NodePort
  ports:
  - name: http
    port: 3000
    nodePort: 30000
  selector:
    app: kotsbots-api
